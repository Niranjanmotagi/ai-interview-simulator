{
  "name": "@ai-interview/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types and DTOs for the AI Interview Simulator.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
